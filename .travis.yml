language: generic 
sudo: required

services:
  -docker

before_install:
  - docker build -t beranrdss/docker-react -f Dockerfile.dev .

script:
  - docker run bernardss/docker-react npm run test -- --coverage